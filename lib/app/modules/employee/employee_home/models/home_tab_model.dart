class HomeTabModel {
  String titleKey;
  bool isSelected;
  bool hasUpdate;

  HomeTabModel(
      {required this.titleKey,
      required this.isSelected,
      required this.hasUpdate,
      });
}
